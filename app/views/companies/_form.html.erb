<%= form_for @company do |f| %>
    <%= f.label :company_name %>
    <%= f.text_field :name %><br>
    <%= f.fields_for :users do |f| %>
       <%= render partial: 'users/nested_user_fields', locals: {f: f} %>
    <% end %>

    <%= f.submit %>
<% end %>