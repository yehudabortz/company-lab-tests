<aside class="menu">
  <%= render 'users/company_dashboard' if current_company %>
  <%= render 'users/lab_dashboard' if current_lab %>
  <%= render 'users/user_dashboard' if current_user && current_user.is_customer %>

  <div class="container pt-5">
    <%= button_to "Delete Account", user_path, method: "delete", class: "button is-small is-danger", data: { confirm: 'Are you sure you want to delete your account? This action can not be undone.' } %>
  </div>

  </aside>
